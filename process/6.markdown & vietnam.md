xây dựng custom pipeline chatbot tiếng Việt, có khả năng truy vấn dữ liệu markdown (ví dụ: tài liệu sản phẩm, blog, hướng dẫn sử dụng). Bạn sẽ cần:

Chọn mô hình LLM tiếng Việt (ví dụ: PhoBERT, vi-LLM, hoặc các mô hình từ Hugging Face hỗ trợ tiếng Việt).
Tiền xử lý dữ liệu markdown: Đọc file markdown, trích xuất nội dung, có thể dùng thư viện như markdown hoặc BeautifulSoup để chuyển đổi sang plain text.
Trong class custom pipeline (kế thừa mlflow.pyfunc.PythonModel), bạn load mô hình tiếng Việt và dữ liệu đã xử lý vào bộ nhớ.
Trong hàm predict, khi nhận câu hỏi, bạn có thể tìm kiếm đoạn văn bản liên quan trong dữ liệu markdown (dùng search đơn giản hoặc embedding semantic search), sau đó truyền context này vào mô hình LLM để sinh câu trả lời tiếng Việt.
Ví dụ khung mã:

